[package]
name = "sim-core"
version = "0.1.0"
edition = "2021"

[dependencies]
glam = "0.29"
bytemuck = { version = "1", features = ["derive"] }
microlp = "0.4"
slotmap = "1"
serde = { version = "1", features = ["derive"] }
ron = "0.8"

[dev-dependencies]
rstest = "0.23"
